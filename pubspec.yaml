name: scroll_mate
description: Multiple scroll controller work synchronization and define how many line to display
version: 1.0.1
homepage: https://github.com/yusufnadar/scroll_mate

environment:
  sdk: '>=2.19.5 <4.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
